<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="2dp"
    android:background="#153859"
    >

  <View
      android:id="@+id/cell_overlay"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_alignBottom="@+id/cell_button"
      android:layout_alignEnd="@id/cell_button"
      android:layout_alignStart="@id/cell_button"
      android:layout_alignTop="@id/cell_button"
      android:background="@drawable/background_overlay"
      />

  <im.ene.ikiro.widget.SquareImageButton
      android:id="@id/cell_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:background="?attr/selectableItemBackgroundBorderless"
      android:padding="16dp"
      android:scaleType="fitCenter"
      tools:src="@drawable/ic_button_shape_oval"
      />

</RelativeLayout>